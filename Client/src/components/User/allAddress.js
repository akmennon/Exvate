import React from 'react'
import { useSelector } from 'react-redux'
import Button from '@material-ui/core/Button'

const Addresses = (props) =>{   //LAST - Show all addresses and option to remove them
    return (
        <div>
            <h1>All Addresses</h1>
        </div>
    )
}

export default Addresses